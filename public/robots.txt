# Basirah — Islamic knowledge for Myanmar Muslims
# https://basirah.pages.dev
#
# This site is public and intended to be discoverable and citable.
# robots.txt expresses crawl preferences; it is not an access-control layer.
# Keep hashed CSS/JS/font/audio assets crawlable because search engines may
# need CSS and JavaScript to render pages correctly.
# AI/search crawlers are allowed by the default rule.

User-agent: *
Allow: /

# Cloudflare's internal analytics and edge endpoints are not site content.
Disallow: /cdn-cgi/

Sitemap: https://basirah.pages.dev/sitemap-index.xml
